version: "3.8"

services:
  react-app:
    build:
      context: .
    container_name: customer-feedback-web
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true
